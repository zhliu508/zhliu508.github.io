# zhliu508.github.io

个人主页静态站点，直接部署到 GitHub Pages 即可。

## 本地预览

直接用浏览器打开 `index.html`。

## 替换背景

默认二次元背景位于 `assets/anime-campus-bg.png`。想换图时，可以直接替换这个文件；也可以在 `index.html` 顶部 CSS 的 `--anime-bg` 变量里改成新的图片路径。
